{
  "name": "Orchestrator",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "user": "ubuntu",
  "install": "./scripts/cloud-agent-install.sh",
  "start": "./scripts/cloud-agent-start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "python3 -m uv run --package orchestrator-api uvicorn orchestrator_api.main:app --reload --host 0.0.0.0 --port 8000"
    },
    {
      "name": "worker",
      "command": "python3 -m uv run --package orchestrator-worker arq orchestrator_worker.tasks.WorkerSettings"
    },
    {
      "name": "web",
      "command": "cd apps/web && npm run dev -- --host 0.0.0.0"
    }
  ],
  "ports": [
    { "name": "API", "port": 8000 },
    { "name": "Web", "port": 5173 }
  ]
}
