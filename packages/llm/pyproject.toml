[project]
name = "orchestrator-llm"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40",
    "orchestrator-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orchestrator_llm"]

[tool.uv.sources]
orchestrator-core = { workspace = true }
